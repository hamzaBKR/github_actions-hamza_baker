name: test_variable
on: push
jobs:
    print:
        runs-on: ubuntu-latest
        env:
            test: variables   
        steps:
            - name: test variable
              run: echo 'test ${{env.test}}'